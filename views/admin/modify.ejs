<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head.ejs') %>

    <body>
        <div class="header-2">
            <div class="container">
                <%- include('../partials/nav.ejs') %>
            </div>
        </div>

        <div class="add">
            <center>
                <a href="/admin/add">
                    <button class="btn">Add Medicine</button>
                </a>
            </center>
        </div>

        <!--div class="small-container">
            <div class="medicines">
                <% if(products.length>0) {
                products.forEach(product => { %>
                    <div class="onemed">
                        <h3>
                            <%= product.name %>
                        </h3>
                    </div>
                    <br>
                    <% })
                 } %>
            </div>
        </div-->
        <% var sum=0;%>
            <% if(products.length>0){%>
                <div class="small-container cart-page">
                    <table>
                        <%products.forEach(product=>{%>
                            <tr>
                                <td>
                                    <div class="cart-info onemed">
                                        <a href="/products/<%=product._id %>"><img src="<%=product.src%>"></a>
                                        <div>
                                            <p>
                                                <%=product.name%>
                                            </p>
                                            <small><%=product.price%>$</small>
                                        </div>
                                    </div>
                                </td>
                                <td>
                                    <a href="/admin/<%=product._id%>"><button class="btn">Edit</button></a>
                                </td>

                            </tr>
                            <%})%>
                    </table>
                </div>
                <%}else{%>
                    <div class="blank">
                        <p>You haven't added any <b>medicines</b>
                        </p>
                        <p>Click on Add Medicines button to add medicines</p>
                        <%}%>

                            <!------------Footer-------------->
                            <%- include('../partials/footer.ejs') %>



    </body>
    <script>
        var MenuItems = document.getElementById("MenuItems");
        MenuItems.style.maxHeight = "0px";

        function menuToggle() {
            if (MenuItems.style.maxHeight == "0px") {
                MenuItems.style.maxHeight = "200px"
            } else {
                MenuItems.style.maxHeight = "0px"
            }
        }
    </script>

</html>